# Chapter 9 benchmark results

Generated by `scripts/render_results.py` from the git-tracked JSON in
`benchmarks/results/`. A run you have not added to git is ignored. That
directory is gitignored, so add the file with `git add -f` first, then
re-run the script.

## Error fingerprint compression

Source: `fingerprint-compression-2026-08-26T015357.json`
Measured: 2026-08-26

| Metric | Value |
|---|---|
| generated.code_paths_recorded | 1200 |
| generated.raw_error_spans | 2000000 |
| generated.zipf_alpha | 0.5 |
| generated.template_space | 1200 |
| measured.distinct_unnormalized | 2000000 |
| measured.distinct_unnormalized_share_of_n | 1.0 |
| measured.distinct_fingerprints | 1200 |
| measured.compression_ratio | 1666.7 |
| measured.top10_share_of_error_volume | 0.7185 |
| measured.busiest_issue_share | 0.3015 |

> What generalizes is the mechanism and the result F == P: the listing 9.2 normalization recovers the seeded code-path count exactly, from raw messages that were 100.0% distinct before it ran. What does not generalize is the 1,667x ratio. That is N divided by the number of code paths seeded here, so it is a property of this generator and not a figure to quote for a service. Line numbers vary across three values per path to stand in for three deploys; dropping the line-number strip from the listing forks each issue into three.

## Metric divergence across the sampler

Source: `sampler-divergence-2026-08-26T013924.json`
Measured: 2026-08-26

| Metric | Value |
|---|---|
| prometheus | http://localhost:9090 |
| grain.service | checkout-service |
| grain.span | fraud.score |
| pre.total | 306.0 |
| pre.errors | 9.0 |
| pre.error_rate | 0.029412 |
| post.total | 14.0 |
| post.errors | 9.0 |
| post.error_rate | 0.642857 |
| inflation_ratio | 21.857 |

> Direction is the claim: post error rate > pre error rate, and pre total > post total. The magnitude is a function of the injected error ratio and the sample rate, not a universal constant.
